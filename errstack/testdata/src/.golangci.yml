linters-settings:
  custom:
    errstack:
      type: "module"
      description: This is an example usage of a plugin linter.
      settings: { }

linters:
  disable-all: true
  enable:
    - errstack
  fast: false