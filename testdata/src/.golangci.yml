linters-settings:
  custom:
    errstack:
      type: "module"
      description: Walks through the AST and finds all functions that return an error.
      settings: { }

linters:
  disable-all: true
  enable:
    - errstack