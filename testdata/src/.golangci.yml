linters-settings:
  custom:
    errstack:
      type: "module"
      description: Finds unnecessary error wraps with stacktraces.
      settings: { }

linters:
  disable-all: true
  enable:
    - errstack